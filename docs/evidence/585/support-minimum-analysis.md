# Server support analysis (plan §5 "Server minimum" / §13)

Generated by `support-minimum.mjs`'s `deriveProposedMinimum()`, invoked from
`run-matrix.mjs` — every claim below is either produced by a live checked search over
this checkout or cites its exact source (a repository file:line, or the installed
package's own README).

## 1. Does the repository currently pin/exercise/promise a ClickHouse version?

No. Checked search over 287 files (CI workflows, deploy/, docker-compose.yaml, README.md, docs/, tests/) found no genuine ClickHouse version pin/promise; 10 version-shaped token(s) were found and excluded with a recorded reason (see pinnedVersionScan.benignExclusions) — none is a repository-exercised promise.

## 2. Server-sensitive dependency inventory

| Feature | Status | Earliest documented support | Raises floor? | Citation |
|---|---|---|---|---|
| JSONStringsEachRowWithProgress meta line (column name/type header the streaming Table parser needs to map row values to columns) | required | NOT long-standing — ClickHouse GitHub PR #74181 ("JSONEachRowWithProgress format will include meta, totals, and extremes", merged 2025-01-06) added this; empirically CONFIRMED ABSENT live against both 24.8.14.39 and 24.8.14.10547.altinitystable (every query tested, including a bare SELECT 1, returns only progress/row lines, never a meta line) and PRESENT on 26.6.2.160/26.3.16.10001.altinitystable — this spike did not test an intermediate 25.x row, so the exact earliest passing release line is not independently confirmed, only bounded (fails on 24.8.14.x, passes by 26.3/26.6) | yes | src/core/stream.ts applyStreamLine(): "if (json.meta) { result.columns = ... } else if (json.row) { ... result.columns.map((c) => row[c.name]) }" — no meta-less fallback; live reproduction: tests/spike/clickhouse-client/live-sessions.test.ts (temporary-table/session-SET row-value assertions) and the KILL QUERY system.processes probe in live-parity.test.ts both fail on both 24.8 rows for exactly this reason (issue #585 Phase 0 evidence review, 2026-08-06) |
| JSONEachRowWithProgress (KPI streaming format) | required | NOT long-standing — same ClickHouse GitHub PR #74181 (merged 2025-01-06) that added the meta line to JSONStringsEachRowWithProgress applies to this sibling format too (both share the underlying progress-format implementation); not independently re-verified for the KPI path specifically by this spike (no KPI-shaped live case in the corpus), but the underlying format change is the same one | no | src/net/ch-client.ts / src/dashboard — KPI execution path (CLAUDE.md "KPI execution uses JSONEachRowWithProgress"); see the JSONStringsEachRowWithProgress entry above for the verified root cause this shares |
| totals/extremes lines (rows_before_limit_at_least, totals, extremes) | optional | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/core/stream.ts applyStreamLine — lines without meta/row/progress/exception are ignored |
| HTTP settings sent by ch-client.ts (max_result_rows, result_overflow_mode, add_http_cors_header, enable_http_compression) | required | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/net/ch-client.ts chUrl()/runQuery() |
| wait_end_of_query (non-streaming reads only) | optional | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/net/ch-client.ts runQuery(): "wait_end_of_query buffers the whole response server-side..." |
| enable_http_compression=1 (gzip/br response compression negotiation) | required | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/net/ch-client.ts chUrl() |
| X-ClickHouse-Summary response header | optional | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/application/export-service.ts and core/stream.ts summary handling |
| X-ClickHouse-Exception-Tag header + tagged late-exception frame | capability-gated | 24.11 | no | README.md "Export" section: "CH ≥ 24.11; older servers fall back to a plain-text scan" — src/core/stream.ts findExceptionFrame(tailLatin1, tag) |
| Legacy untagged late-exception detection (plain-text "Code: N. DB::Exception:" scan) | required (as the fallback path for pre-24.11 servers) | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/core/stream.ts findExceptionFrame — untagged branch |
| Native query parameters (param_<name> query-string args) | optional | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/net/ch-client.ts queryJson()/runQuery() params; README.md "Query variables" |
| Roles (role=<name> query-string arg) | optional | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | tests/spike/clickhouse-client/current-adapter.ts nativeParamsForCurrent() role handling |
| Sessions (session_id query-string arg) | optional | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/application/query-execution-service.ts ScriptStatement.params session_id |
| SESSION_IS_LOCKED (code 373) retry classification | required (for the retry-safety invariant, whenever a session IS used) | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/application/query-execution-service.ts SESSION_BUSY regex |
| KILL QUERY WHERE query_id = ... ASYNC | optional (best-effort cancellation only) | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/net/ch-client.ts killQuery()/killQueryWithLease() |
| DataLakeCatalog visibility setting (show_data_lake_catalogs_in_system_tables / renamed show_remote_databases_in_system_tables) | capability-gated | 25.8 | no | src/net/ch-client.ts querySystemAware() docstring |
| Older system-table fallback shape generally (system.tables/system.columns queried without the data-lake setting) | required (as the universal fallback) | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/net/ch-client.ts querySystemAware() plain() branch |
| KPI named-tuple detection (Tuple(value, delta) returned as a JSON object vs. a positional array/string) | capability-gated | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/core/kpi.ts readKpiFields() — kpi-server-named-tuple-unsupported diagnostic; tests/unit/kpi.test.ts serverVersion display case |
| Variable-option batch reads (tupleElement(tuple(*), n) positional projection for __variable_name/__variable_value/__variable_label) | required | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/core/variable-options.ts — "Both functions have existed since ClickHouse 20.4 ... the alternative, a positional wire format, first shipped in 25.2 and would have made the whole feature fail on anything older." |
| Authentication error code/message forms (401/403, AUTHENTICATION_FAILED code 516, isAuthExpiredBody text matching) | required | not independently documented; long-standing ClickHouse HTTP-interface behavior, predates every evaluated matrix row (oldest evaluated: 24.8) | no | src/net/ch-client.ts authedFetch(); src/core/stream.ts isAuthExpiredBody() |

## 3. Official-client documented guaranteed floor

`@clickhouse/client-web` (node_modules/@clickhouse/client-web/README.md, "## Compatibility with ClickHouse" table row "1.12.0+ | 24.8+") documents a guaranteed floor of ClickHouse **24.8+**.

## 4. Derivation

- Checked search over 287 files (CI workflows, deploy/, docker-compose.yaml, README.md, docs/, tests/) found no genuine ClickHouse version pin/promise; 10 version-shaped token(s) were found and excluded with a recorded reason (see pinnedVersionScan.benignExclusions) — none is a repository-exercised promise.
- Official client node_modules/@clickhouse/client-web/README.md, "## Compatibility with ClickHouse" table row "1.12.0+ | 24.8+" documents a guaranteed floor of ClickHouse 24.8+.
- Application inventory raises the floor to NOT long-standing — ClickHouse GitHub PR #74181 ("JSONEachRowWithProgress format will include meta, totals, and extremes", merged 2025-01-06) added this; empirically CONFIRMED ABSENT live against both 24.8.14.39 and 24.8.14.10547.altinitystable (every query tested, including a bare SELECT 1, returns only progress/row lines, never a meta line) and PRESENT on 26.6.2.160/26.3.16.10001.altinitystable — this spike did not test an intermediate 25.x row, so the exact earliest passing release line is not independently confirmed, only bounded (fails on 24.8.14.x, passes by 26.3/26.6) via: JSONStringsEachRowWithProgress meta line (column name/type header the streaming Table parser needs to map row values to columns).
- Proposed minimum = max(pinned official-client guaranteed minimum=24.8, application feature/fallback minimum=NOT long-standing — ClickHouse GitHub PR #74181 ("JSONEachRowWithProgress format will include meta, totals, and extremes", merged 2025-01-06) added this; empirically CONFIRMED ABSENT live against both 24.8.14.39 and 24.8.14.10547.altinitystable (every query tested, including a bare SELECT 1, returns only progress/row lines, never a meta line) and PRESENT on 26.6.2.160/26.3.16.10001.altinitystable — this spike did not test an intermediate 25.x row, so the exact earliest passing release line is not independently confirmed, only bounded (fails on 24.8.14.x, passes by 26.3/26.6), earliest version that passed every required hard gate (live matrix)=26.3.16.10001.altinitystable) = 26.3.16.10001.altinitystable (binding source: earliest version that passed every required hard gate (live matrix)).
- Live-gate corroboration (step 5 of the plan's derivation) has run and is folded in above.

**Proposed minimum: ClickHouse 26.3.16.10001.altinitystable** (binding source: earliest version that passed every required hard gate (live matrix)).

Live-gate corroboration (plan §5 step 5): earliest passing version fed back = 26.3.16.10001.altinitystable.
